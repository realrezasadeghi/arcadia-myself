/**
 * @deprecated This component is no longer used.
 * The project list page now uses:
 * - ProjectToolbar (server component with SearchInput client component)
 * - ProjectListSection (server component)
 * - ProjectCardWrapper (client component for dialogs)
 */
export function ProjectContainer() {
  return null;
}
