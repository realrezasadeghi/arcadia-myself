import { LoginView } from "@/modules/auth/ui/views/login";

export default function LoginPage() {
  return (
    <div className="relative min-h-screen flex items-center justify-center bg-muted/30 p-4">
      <LoginView />
    </div>
  );
}
